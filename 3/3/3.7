#include <cstring>
#include <iostream>

using namespace std;

void len(const char* str, unsigned int &l){
    l = strlen(str);
    // return p;
}

void cmp(const char* str1, const char* str2, int &c, int n, int y){
    if(y==1){
        c = strcmp(str1, str2);
    }
    if(y==2){
        c = strncmp(str1, str2, n);
    }
}

void chr()

int main()
{
    const char str[] = "pjkjk", str1[] = "hello", str2[] = "hell";
    unsigned int l, count;
    int c, n=4, y;
    cin >> count;
    if(count == 1){
        len(str, l);
        cout << "\n" << l;
    }
    
    if(count == 2){
        cin >> y;
        cmp(str1, str2, c, n, y);
        cout << "\n" << c;
    }
    return 0;
}
